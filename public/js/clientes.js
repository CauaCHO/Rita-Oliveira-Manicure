import { Store } from './storage.js';
import { requireLogin, adminLayout } from './admin-common.js';
import { $, formatDateBR, money, whatsappLink } from './utils.js';
requireLogin();
adminLayout('clientes', `<div class="card-title"><div><h2 style="margin:0">Clientes</h2><p class="muted">Ranking de quem mais agenda e histórico de atendimentos.</p></div></div><section id="clientList" class="table-list"></section><div id="modal" class="modal"><div class="modal-content"><div class="card-title"><h3 id="title">Histórico</h3><button id="close" class="btn btn-light"><i class="fa-solid fa-xmark"></i></button></div><div id="history"></div></div></div>`);
function render(){ const box=$('#clientList'); const clients=Store.listClients(); box.innerHTML=clients.length?'':'<div class="empty">Nenhuma cliente registrada ainda.</div>'; clients.forEach((c,i)=>{ const item=document.createElement('div'); item.className='list-item'; const msg=`Olá, ${c.nome}! Tudo bem? 😊`; item.innerHTML=`<div><h4>${i+1}. ${c.nome||'Sem nome'}</h4><p>${c.telefone||'Sem WhatsApp'} • ${c.totalAgendamentos} agendamento(s) • ${money(c.totalGasto)}</p></div><div class="btn-row"><button class="btn btn-light">Histórico</button>${c.telefone?`<a class="btn btn-info" target="_blank" href="${whatsappLink(c.telefone,msg)}"><i class="fa-brands fa-whatsapp"></i> WhatsApp</a>`:''}</div>`; item.querySelector('button').onclick=()=>openHistory(c); box.appendChild(item); }); }
function openHistory(c){ $('#title').textContent=`Histórico de ${c.nome}`; const aps=Store.listClientAppointments(c).sort((a,b)=>`${b.data} ${b.hora}`.localeCompare(`${a.data} ${a.hora}`)); $('#history').innerHTML=aps.length?'':'<div class="empty">Sem histórico.</div>'; aps.forEach(a=>{$('#history').innerHTML += `<div class="list-item"><div><h4>${formatDateBR(a.data)} às ${a.hora}</h4><p>${a.servicoNome||'Sem serviço'} • ${money(a.valor)} • ${a.statusAgenda}</p></div></div>`;}); $('#modal').classList.add('open');}
$('#close').onclick=()=>$('#modal').classList.remove('open'); render();
