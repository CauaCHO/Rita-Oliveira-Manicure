import { Store } from './storage.js';
import { requireLogin, adminLayout } from './admin-common.js';
import { $, todayISO, money, formatDateBR } from './utils.js';
requireLogin();
const monthNow=todayISO().slice(0,7);
adminLayout('relatorios', `<div class="card-title"><div><h2 style="margin:0">Relatórios</h2><p class="muted">Faturamento, pendências e clientes que mais agendam.</p></div></div><section class="card"><div class="field"><label>Mês</label><input id="month" type="month" value="${monthNow}"></div></section><section id="stats" class="grid grid-3" style="margin-top:14px"></section><div class="grid grid-2"><section class="card"><h3>Serviços mais feitos</h3><div id="services"></div></section><section class="card"><h3>Clientes que mais agendam</h3><div id="clients"></div></section></div><div class="section-title">Agendamentos do mês</div><section id="appointments" class="table-list"></section>`);
function render(){ const r=Store.monthlyReport($('#month').value); $('#stats').innerHTML=`<div class="card stat"><div class="stat-icon"><i class="fa-solid fa-calendar"></i></div><div><span class="muted small">Agendamentos</span><br><b>${r.totalAppointments}</b></div></div><div class="card stat"><div class="stat-icon"><i class="fa-solid fa-sack-dollar"></i></div><div><span class="muted small">Recebido</span><br><b>${money(r.received)}</b></div></div><div class="card stat"><div class="stat-icon"><i class="fa-solid fa-hourglass-half"></i></div><div><span class="muted small">Pendente</span><br><b>${money(r.pending)}</b></div></div>`; $('#services').innerHTML=r.byService.length?'':'<p class="muted">Sem dados.</p>'; r.byService.forEach(([n,q])=>$('#services').innerHTML+=`<div class="list-item"><div><h4>${n}</h4><p>${q} vez(es)</p></div></div>`); $('#clients').innerHTML=r.byClient.length?'':'<p class="muted">Sem dados.</p>'; r.byClient.forEach(([n,q])=>$('#clients').innerHTML+=`<div class="list-item"><div><h4>${n}</h4><p>${q} agendamento(s)</p></div></div>`); $('#appointments').innerHTML=r.appointments.length?'':'<div class="empty">Nenhum agendamento neste mês.</div>'; r.appointments.forEach(a=>$('#appointments').innerHTML+=`<div class="list-item"><div><h4>${formatDateBR(a.data)} às ${a.hora} - ${a.clienteNome||a.statusAgenda}</h4><p>${a.servicoNome||'Sem serviço'} • ${money(a.valor)} • ${a.statusAgenda} • pagamento: ${a.statusPagamento}</p></div></div>`); }
$('#month').onchange=render; render();
